fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
streamlit==1.31.0
plotly==5.18.0
requests==2.31.0
pillow==10.2.0

# PyTorch (instalar separadamente via conda)
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
# ou para CPU: conda install pytorch torchvision torchaudio cpuonly -c pytorch
